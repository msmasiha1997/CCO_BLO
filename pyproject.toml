[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cco-blo"
version = "0.1.0"
description = "Chance-constrained optimization via bilevel (scalar) quantile reformulation + hypergradients."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "code"}

[tool.setuptools.packages.find]
where = ["code"]
